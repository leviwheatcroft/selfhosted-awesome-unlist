footer
  .container
    .row
      .column
        | Total Projects: #{projectCount}
      .column
        | Built At: #{moment(buildDate).format('DD MMM YYYY')}
      .column
        | Version: #{version}
